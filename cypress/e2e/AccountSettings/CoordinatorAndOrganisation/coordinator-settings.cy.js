// import * as data from '../../fixtures/test-data.json';

describe('Coordinator settings', () => {
  beforeEach(() => {
    cy
      .visit('/')
      .byPassAuthen();
  });

  it('Comming soon', () => {
  });
});
